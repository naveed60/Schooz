import { resolveSchoolContextCached } from '@/server/authorization';
import { listExamDocuments } from '@/server/exam-documents/service';
import { uploadExamDocumentAction, deleteExamDocumentAction, downloadExamDocumentAction } from '@/server/exam-documents/actions';
export default async function ExamDocuments({ params }: { params: Promise<{ schoolSlug: string; examId: string }> }) { const { schoolSlug, examId } = await params; const c = await resolveSchoolContextCached(schoolSlug); const docs = await listExamDocuments(c, examId); const manage = c.permissions.includes('exams:manage') || c.permissions.includes('exams/results:manage'); return <main><h1>Exam documents</h1>{manage && <form action={uploadExamDocumentAction} encType='multipart/form-data'><input type='hidden' name='schoolSlug' value={schoolSlug}/><input type='hidden' name='examId' value={examId}/><select name='type'><option>QUESTION_PAPER</option><option>ANSWER_KEY</option><option>MARK_SHEET</option><option>SYLLABUS</option><option>INSTRUCTIONS</option><option>OTHER</option></select><input name='file' type='file' required/><button type='submit'>Upload</button></form>}<ul>{docs.map(d=><li key={d.id}>{d.type} — {d.originalFileName} ({d.sizeBytes} bytes)<form action={downloadExamDocumentAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input type='hidden' name='documentId' value={d.id}/><button type='submit'>Download</button></form>{manage && <form action={deleteExamDocumentAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input type='hidden' name='examId' value={examId}/><input type='hidden' name='documentId' value={d.id}/><button type='submit'>Delete</button></form>}</li>)}</ul></main>; }
