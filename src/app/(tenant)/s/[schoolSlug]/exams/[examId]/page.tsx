import { resolveSchoolContextCached } from '@/server/authorization';
import { getExam } from '@/server/exams/service';
import { setExamStatusAction } from '@/server/exams/actions';
export default async function ExamDetail({ params }: { params: Promise<{ schoolSlug: string; examId: string }> }) { const { schoolSlug, examId } = await params; const c = await resolveSchoolContextCached(schoolSlug); const exam = await getExam(c, examId); const manage = c.permissions.includes('exams:manage') || c.permissions.includes('exams/results:manage'); return <main><h1>{exam.name}</h1><p>{exam.examType} · {exam.status} · {exam.startDate.toISOString().slice(0,10)} to {exam.endDate.toISOString().slice(0,10)}</p>{manage && <form action={setExamStatusAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input type='hidden' name='examId' value={exam.id}/><select name='status' defaultValue='SCHEDULED'><option value='SCHEDULED'>Schedule</option><option value='ONGOING'>Start</option><option value='COMPLETED'>Complete</option><option value='ARCHIVED'>Archive</option></select><button type='submit'>Change status</button></form>}<p><a href={'/s/' + schoolSlug + '/exams/' + exam.id + '/documents'}>Documents</a></p><h2>Schedules</h2><ul>{exam.schedules.map(s=><li key={s.id}>{s.examDate.toISOString().slice(0,10)} — {s.academicClass.name} — {s.subject.name} ({s.maxMarks.toString()} marks)</li>)}</ul></main>; }
