import { resolveSchoolContextCached } from '@/server/authorization';
import { listAcademicYears } from '@/server/academics/service';
import { listExams } from '@/server/exams/service';
import { createExamAction, createGradingSchemeAction } from '@/server/exams/actions';
export default async function ExamsPage({ params }: { params: Promise<{ schoolSlug: string }> }) { const { schoolSlug } = await params; const c = await resolveSchoolContextCached(schoolSlug); const [years, exams] = await Promise.all([listAcademicYears(c), listExams(c)]); const manage = c.permissions.includes('exams:manage') || c.permissions.includes('exams/results:manage'); return <main><h1>Exams</h1>{manage && <form action={createExamAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><select name='academicYearId'>{years.map(y=><option key={y.id} value={y.id}>{y.name}</option>)}</select><input name='name' placeholder='Exam name' required/><input name='examType' placeholder='Exam type' required/><input name='startDate' type='date' required/><input name='endDate' type='date' required/><button type='submit'>Create exam</button></form>}<ul>{exams.items.map(e=><li key={e.id}><a href={'/s/' + schoolSlug + '/exams/' + e.id}>{e.name}</a> — {e.status}</li>)}</ul>{manage && <form action={createGradingSchemeAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input name='name' placeholder='Grading scheme name' required/><input name='items' placeholder='JSON grading ranges' required/><label><input name='isDefault' type='checkbox'/> Default</label><button type='submit'>Create grading scheme</button></form>}</main>; }
