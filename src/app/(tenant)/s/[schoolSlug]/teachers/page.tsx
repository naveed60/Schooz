import { resolveSchoolContextCached } from '@/server/authorization';
import { listTeachers } from '@/server/teachers/service';
import { createTeacherAction, deactivateTeacherAction } from '@/server/teachers/actions';
export default async function TeachersPage({ params, searchParams }: { params: Promise<{ schoolSlug: string }>; searchParams: Promise<{ q?: string; page?: string }> }) { const { schoolSlug } = await params; const sp = await searchParams; const c = await resolveSchoolContextCached(schoolSlug); const result = await listTeachers(c, { query:sp.q, page:Number(sp.page ?? '1') || 1 }); const manage = c.permissions.includes('teachers:manage'); return <main><h1>Teachers</h1>{manage && <form action={createTeacherAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input name='employeeNumber' placeholder='Employee number' required/><input name='firstName' placeholder='First name' required/><input name='lastName' placeholder='Last name' required/><input name='joinDate' type='date' required/><input name='email' type='email' placeholder='Email'/><input name='designation' placeholder='Designation'/><button type='submit'>Create teacher</button></form>}<form><input name='q' defaultValue={sp.q} placeholder='Search teachers'/><button type='submit'>Search</button></form><ul>{result.items.map(t => <li key={t.id}><a href={'/s/' + schoolSlug + '/teachers/' + t.id}>{t.employeeNumber} — {t.firstName} {t.lastName} ({t.status})</a>{manage && t.status === 'ACTIVE' && <form action={deactivateTeacherAction}><input type='hidden' name='schoolSlug' value={schoolSlug}/><input type='hidden' name='teacherId' value={t.id}/><button type='submit'>Deactivate</button></form>}</li>)}</ul><nav><a href={'?q=' + (sp.q ?? '') + '&page=' + Math.max(result.page - 1, 1)}>Previous</a> <a href={'?q=' + (sp.q ?? '') + '&page=' + (result.page + 1)}>Next</a></nav></main>; }
